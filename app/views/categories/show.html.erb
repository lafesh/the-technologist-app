<h1>Browse by Category</h1>

<% @categories.each do |category| %>
<h2><%= link_to category.reviews(:category_id), category_reviews_path(category.reviews) %></h2><br /> 
<%= category.title.titleize %><br />
<% end %>

