<h1>Publish a Review</h1>

<%= form_for @review do |f| %>

  <%= f.label :category %>
  <%= f.collection_select :category_id, Category.order(:title), :id, :title %> <br />


  <%= f.label :title %>
  <%= f.text_field :title %>
  <br />

  <%= f.label :content %>
  <%= f.text_area :content %>
  <br />

  <%= f.submit "Publish Review" %>
  <% end %>

